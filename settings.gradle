pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id('io.quarkus') version '3.28.1'
    }
}

rootProject.name = 'eos-java'

include 'versions'
include 'versions-test'
include 'devices'
include 'optimization'
// include 'service-eos'
